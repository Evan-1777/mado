# Project

> **本文件是项目的「单一事实来源」**，供 AI Agent 动手前快速建立全局认知。
>
> - **维护原则**：代码库变更后必须同步本文件——对照 §0 维护速查判断要改哪节，而非凭感觉。
> - **边界**：项目定位与设计原则见 `SCOPE.md`，工作流规则见 `AGENTS.md`，本文件不重复，仅在需要处引用。
> - **填写约定**：带 `<!-- 待填 -->` 为占位项，写完即删注释；标 `（可选）` 的节若无内容请**整节删除**（勿留空节）；`★` 标记易错点。

## 0. 维护速查

> 代码库变更后，按下表定位需同步更新的章节。未列出的变更类型默认无需改本文件。

| 变更类型 | 需更新章节 |
|----------|-----------|
| 新增 / 升级 / 移除库依赖 | §2 环境与运行（属关键选型时另记 §8） |
| 新增 / 重构 / 删除模块 | §3 目录结构、§4 架构与数据流 |
| 调整命名 / 编码 / 日志规范 | §5 关键约定 |
| 踩到新坑或确立新约束 | §6 约束与已知坑 |
| 引入 / 切换 / 下线外部服务或脚本 | §7 外部依赖与集成 |
| 关键技术选型定型 | §8 决策记录 |
| 引入项目特有名词 | §9 术语表 |
| 运行 / 启动 / 测试方式变化 | §2 环境与运行 |

## 1. 概述

- **一句话定位**：<!-- 待填：项目是什么、解决什么问题；与 SCOPE.md 定位一致，一句话不展开 -->
  - ✅ 好：「本地运行的命令行书签管理器」
  - ❌ 坏：三段话重复 SCOPE.md 已写过的设计原则
- **当前阶段**：<!-- 待填：初始化 / 开发中 / 维护中 -->
- **非目标（不做什么）**：<!-- 待填：明确边界，防止 AI 过度设计；若 SCOPE.md 已详述，此处仅引用不展开 -->

## 2. 环境与运行

> AI 最易踩坑处。所有「换台机器就不一样」的前提都写在这里。**环境类坑在此内联标注**，架构 / 历史类坑集中到 §6。

- **运行平台**：<!-- 待填：操作系统 / 是否本地运行 / 是否容器 -->
  - ★ 易错：跨平台差异（路径分隔符、换行符、大小写敏感）在此注明
- **Shell**：<!-- 待填：如 PowerShell / Git Bash -->
  - ★ 易错：路径含中文 / 空格须加引号；不同 shell 变量语法不同（`$VAR` vs `$env:VAR`）
  - ✅ 好：「PowerShell 7（终端默认）」
  - ❌ 坏：「终端」（哪个？版本？）
- **版本管理**：<!-- 待填：是否 git 仓库、分支策略 -->
- **语言 / 运行时**：<!-- 待填：含版本号 -->
  - ✅ 好：「Node.js 20.11.0」
  - ❌ 坏：「Node」（版本直接影响 API 可用性）
- **依赖管理**：<!-- 待填：依赖清单文件位置，或「无」 -->
- **如何运行**：<!-- 待填：启动命令 -->
  - ★ 易错：写全前置步骤（source env / 激活虚拟环境 / 先 build），否则换环境必失败
  - ✅ 好：「`npm run dev`（须先 `cp .env.example .env`）」
  - ❌ 坏：「启动服务」（怎么启动？前置？）/ 写死绝对路径
- **如何测试**：<!-- 待填：测试命令；无测试框架则写「手动验证 + 验证点」 -->
  - ✅ 好：「`npm test`；无框架时手动验证：改完跑 `npm run dev`，看 `/health` 返回 200」
  - ❌ 坏：「跑测试」（哪条？验证什么？）

## 3. 目录结构与模块职责

> 让 AI 无需全盘扫描即可定位代码。仅列关键文件 / 目录，一行说清职责。

```
<!-- 待填：项目树，每个关键条目后用 # 注明职责 -->
```

- ✅ 好：`src/auth/  # 认证模块：登录、JWT 签发与校验`
- ❌ 坏：把 `node_modules/`、`.git/`、构建产物等无信息量目录贴进来

## 4. 架构与数据流

- **核心模块**：<!-- 待填：模块名 → 职责 → 对外接口 -->
- **数据流**：<!-- 待填：输入从哪来 → 经过哪些模块 → 输出到哪去 -->
- **模块依赖**：<!-- 待填：依赖方向，标出禁止反向依赖的边界 -->
  - ★ 易错：依赖方向是架构红线，AI 重构时易无意反向依赖；明确写出「X 禁止依赖 Y」

## 5. 关键约定

> 编码 / 命名 / 组织上的项目内规矩，保证新代码风格一致。设计总原则见 `SCOPE.md`，此处只记可执行的具体约定。

- **命名约定**：<!-- 待填：如文件 kebab-case、函数 camelCase -->
- **注释 / 文档语言**：<!-- 待填 -->
  - ★ 易错：须与 `AGENTS.md` 的「用户面中文 / 内部英文」对齐，勿自创
- **错误处理 / 日志方式**：<!-- 待填 -->

## 6. 约束与已知坑

> 「为什么这么做」的显式记录，防止 AI 好心改坏。**每条必须带原因**——没原因的禁令 AI 无法判断严重性。环境类坑已在 §2 内联，本节记架构 / 历史 / 外部类坑。

- <!-- 待填 -->
- 格式示范：
  - 「`paths.go` 由 `go:generate` 生成，禁止手改——原因：下次 generate 会覆盖」
  - 「`axios` 锁定 1.x，禁止升 2.x——原因：2.x 移除拦截器 API，会破坏 `src/http.ts`」
- ✅ 好：规则 + 原因
- ❌ 坏：「别改 `paths.go`」（为什么？改坏会怎样？）

## 7. 外部依赖与集成（可选）

> 无外部集成时整节删除，勿留空节。

- **第三方服务 / API**：<!-- 待填：服务名、用途、凭据存放位置（不写凭据本身） -->
  - ★ 易错：凭据绝不写入本文件或代码，只记存放位置（如 `.env`、系统密钥库）
- **本地工具脚本**：<!-- 待填：脚本名 → 用途 -->

## 8. 决策记录（可选）

> 跨会话保持一致的关键技术选型。仅记「换一个会引发连锁影响」的决定，一次性小事不必记。无则整节删除。

- 格式：`YYYY-MM-DD 决策——理由`
- ✅ 好：「2026-06-25 选 PostgreSQL 而非 MySQL——理由：RBAC 多对多查询性能更优」
- ❌ 坏：「用了 PostgreSQL」（没理由 = 下次没人知道为何不能换）

## 9. 术语表（可选）

> 项目特有 / 易误解名词，避免 AI 望文生义。通用技术术语不必记。无则整节删除。

- 格式：`术语 = 定义`
- <!-- 待填 -->
