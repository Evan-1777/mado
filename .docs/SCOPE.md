全局备注：
1. 个人项目非商业，在个人PC/个人VPS部署使用，避免不需要的冗余设计
2. 遵循简洁、强逻辑的模块化架构，和简洁明了的代码设计

项目备注：

1. 优秀的的可拓展架构是第一关键，轻量和轻占用是第二关键
2. 项目名称为Mado，一个个人个性化化的Markdown编辑器
3. 开发环境（2026-08-15 迁移至 Linux x86_64）：前端包管理一律 npm（环境无 pnpm，不保留 packageManager 字段与 pnpm-lock.yaml）；允许本机安装并使用 Go（用户级安装于 ~/.local/go，勿装多版本），本机编译与测试放开；Rust 仍不使用；Windows 产物构建与发布验收仍由云端 CI 负责，本机不做 wails 全量构建
4. 测试遵循最小占用（内存/性能/储存）：go test 不带 -race 等重负载旗标，共享默认模块与构建缓存；前端仅做零产物 esbuild 语法检查（--loader:.css=empty，无输出文件）；不缓存/不保留任何一次性构建产物
